"use strict";

// alert("hello")
// in node js we can't use this alert. we can use this only in browser

console.log(3+3)
let name = 'bhavya'
let age = 18
let isLoggedIn = false

// null is a standalone value
// undefined

// suppose consider a weather app. If you are unable to fetch data u won't give 0 u give null
console.log(typeof(null))
console.log(typeof(undefined))